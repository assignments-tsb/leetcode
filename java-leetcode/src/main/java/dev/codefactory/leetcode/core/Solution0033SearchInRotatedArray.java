package dev.codefactory.leetcode.core;

public class Solution0033SearchInRotatedArray {
    public int search(int[] nums, int target) {
        //find the pivot
        int right = 0;
        int left = nums.length-1;
        int mid = (left + right) / 2;

        while (left < right) {

        }

        //binary search
        return -1;
    }
}
